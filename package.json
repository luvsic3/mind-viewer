{
  "name": "mind-viewer",
  "version": "1.1.2",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "ts-standard",
    "build": "preact build",
    "dev": "preact watch"
  },
  "license": "MIT",
  "devDependencies": {
    "preact-cli": "^3.0.5",
    "ts-standard": "^10.0.0",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "preact": "^10.5.12"
  },
  "files": [
    "dist"
  ]
}
